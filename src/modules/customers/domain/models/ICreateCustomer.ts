/*quando vou criar um cliente. só preciso de name e email.
Nao mudei o codigo, por isso nao utilizei essa estrutura em outra parte do codigo */
export interface ICreateCustomer {
  name: string;
  email: string;
}
